import { colors } from './colors';
import { layoutSize } from './layoutSize';
import { fontFaces, fontSizes } from './fonts';
import { images } from './images';

export const theme = {
  ...colors,
  ...layoutSize,
  ...fontFaces,
  ...fontSizes,
  ...images,
};
